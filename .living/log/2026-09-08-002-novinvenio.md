---
session_id: 2026-09-08-002
project: novinvenio
branch: "main"
started: 2026-09-08T09:21:48-0700
ended:
duration_minutes:
files_changed:
---

## Session Log

### 09:21 — Session started
- Branch: `main`
- Resuming from: 2026-09-08-001-novinvenio.md

### 20:15 — Built bin/ni scaffolding tool (issue #76, full scope)
- Command: wrote bin/ni (Python, `ni init <dir> --reference <NII-checkout>`), verified with `python3 -m py_compile`.
- Result: copies the full NovInvenio_Investigations reusable toolchain (deploy/CI plumbing + UniProt study-building scripts, ~19 files) from a live reference checkout rather than an embedded frozen copy, specifically to avoid repeating the bin/sync_reports.sh view/ staleness bug found earlier this session (NII issue #5).
- Output: pixi.toml/conf/domains.yaml rendered fresh per target repo; everything else copied verbatim via shutil.copy2 with mode preserved.

## Session Summary

Session ended without a clean handoff (no `ended`/`duration_minutes` recorded) —
reconstructed from the log content itself in a later session (2026-09-09).

- Built `bin/ni` (NovInvenio issue #76): the `ni init` scaffolding CLI and its
  hermetic test suite (`tests/test_ni.py`, 9 tests against a synthetic reference
  tree). Merged to `main` via PR #91 in the following session, along with the
  end-to-end validation that surfaced two real bugs in the reference toolchain
  (missing `assets/logo/` in the manifest, hardcoded "NovInvenio Investigations"
  site name in `lib/site_pages.py`) — both fixed upstream in NII, not papered
  over in `bin/ni` itself.
- No other work items were logged before the session ended.
