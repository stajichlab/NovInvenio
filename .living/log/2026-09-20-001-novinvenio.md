---
session_id: 2026-09-20-001
project: novinvenio
branch: "main"
started: 2026-09-20T10:39:42-0700
ended:
duration_minutes:
files_changed:
---

## Session Log

### 10:39 — Session started
- Branch: `main`
- Resuming from: 2026-09-19-001-novinvenio.md
### 21:05 — Paralog/novelty disqualifier: problem analysis and option space
- Command: offline replication of `build_presence_matrix.py` filters 1+2 against
  `NovInvenio_Investigations/results/pezizo_set1/search_cache/` (3393 candidates).
- Result: confirmed filter 2 (relative, no floor) erases a tree-confirmed ortholog for
  CorA/A7UWR3 (6/6 outgroup hits, 3.1e-116..4.9e-87) while HEX1 (1/6, 3.4e-12) is a
  genuine absence. Measured that the query/paralog delta does NOT separate the two
  populations (median 10-23 orders in every query-E bucket); an absolute floor does.
  Also confirmed filter 2 is load-bearing — removing it costs HEX1 its candidacy.
- Output: docs/superpowers/specs/2026-09-20-paralog-novelty-disqualifier-analysis.md
  (pre-spec brainstorm notes; no design approved, nothing implemented).


### 22:40 — #128 implemented: absolute floor under filter 2, delta arm rejected
- Command: TDD on bin/build_presence_matrix.py; full suite via `pixi run pytest tests/ -q`.
- Result: 656 passed, 3 skipped, 1 failed. The single failure
  (test_novelty_discovery_integration::test_novelty_discovery_and_screen_end_to_end,
  MMSEQS_FAMILY_CLUSTER exit 132 = SIGILL) reproduces identically on the baseline tag in a
  clean worktree and this node has no avx2 flag — pre-existing hardware issue (L-3), not
  this change.
- Key reversal: the delta arm approved earlier in the session was measured non-selective
  and demoted to opt-in. Rescue fires per cell, so the trigger is MIN delta across a
  candidate\'s suppressed cells (median 8.6), making delta<45 fire for 291/334.
- Output: commit 705898a on branch 128-paralog-rescue-delta; tag
  baseline/pre-paralog-rescue-2026-09-20 (local, unpushed); issues #128 #129 #135;
  docs/superpowers/specs/2026-09-20-paralog-novelty-disqualifier-analysis.md Sec 6-7.


### 12:15 — #135 resolved; --diamond_sensitivity added (PR #149); A/B benchmark launched
- Command: measured TBLASTN-only population vs GFF3 + proteome + diamond raw hits (session-scratch scripts); TDD on nextflow.config/modules/diamond.nf/main.nf in isolated worktree diamond-sensitivity.
- Result: only 3.2% of 1479 tblastn-only cells are intergenic (true FP signature); 56.9% are a diamond default-mode sensitivity gap (zero hits even to E=0.01). Suite 734 passed/4 skipped/0 failed. Benchmark jobs 28961063 (default) + 28961064 (very-sensitive) submitted from the worktree, not the live main checkout.
- Output: PR #149; NI_Sweep/diamond_sensitivity/{run_benchmark.sh,compare.py}; issue #135 comment; todo/diamond-very-sensitive-main-search.md bumped to high.

### 19:07 — #129 implemented and PR'd in isolated worktree
- Command: TDD in NovInvenio-worktrees/alignment-metrics (isolated from the live sweep checkout); widened diamond/blastp --outfmt, extended lib/hits.py parse_tabular (column-count dispatch, backward compat), fixed a real gap where bin/parse_hits.py silently discarded the new fields before build_presence_matrix.py ever saw them.
- Result: 745 passed, 4 skipped, 0 failed. ruff clean. phmmer split out as #150 (structurally different, no domtblout/pident support yet).
- Output: PR #151 (closes #129); issue #150 filed.
